# 2026-09-03 공식 업데이트 후 수정

> **이 문서 = 지금의 정본 1벌.** 계획서이자 작업일지이며, 다 끝나면 그대로 백서로 남아 다음 문서로 이어진다.
> 작업할 때는 **이 문서 하나만** 본다. 옛 문서는 지우지 않고 아카이브로 둔다(완료 이력이 거기 있다).
>
> - 직전 정본 = [`docs/2026-08-14 실사용자 관점 AAB·IPA 앱버전 최적화.md`](2026-08-14%20실사용자%20관점%20AAB·IPA%20앱버전%20최적화.md) (1,388줄, 아카이브)
> - 그 이전 = [`docs/2026-08-25 Tripis v1 안정화.md`](2026-08-25%20Tripis%20v1%20안정화.md) (B4 엔진 기록, 아카이브)
> - 미완료 항목의 `(옛 nnn)` = 직전 정본의 줄 번호 = 원래 맥락을 찾을 자리.

---

## 0. 이 문서의 시작점 — 2026-09-03 완료분

| 묶음 | 커밋 | 내용 |
|---|---|---|
| 여정 엔진 v26 | `a48fa86` | DB-only 동선 재설계(25km 안 먼저·기차 연결·저녁 마지막 슬롯) · MIX 제미니 시각/체류 그대로 반영 · 문지기 좌표동점·PID 쌍둥이 규칙 · 옛 동선 코드 3파일 완전삭제 |
| BTS 카드 + 창고 도구 | `0e2e6b2` | 카드 = 메인앱 버킷 그대로 노출(중복 정렬 삭제) · 사진 무료(구글맵 공개페이지) 우선 → PM → TS · TS 잔량 게이트 정상화 · 손님상 게이트 베스트 RC 면제 |
| 관리자 대시보드 | `83bdc3b` | 로그인 = 비밀번호만 맞으면 통과(자동대문자·공백 차단, 서버도 trim) · eas.json 도메인 주입 · 활동지표 플러스 유지(30초 창 변화 0이면 오늘 누적) |

**현재 상태** = 3건 전부 origin/main 반영(`7829e65`). 안드로이드 관리자 로그인 = 사장님 실기기 **정상 확인**. 관리자 대시보드 = **현재까지 이상 없음**.

---

## 1. 지금 살아 있는 일

### 1-1. 사장님 결정 대기
- [ ] **BTS 전용 분기** = 엔진은 같으나 메인앱과 BTS 가 조금 다르게 돌도록 분기를 만든다(지금 MIX·DB-only 를 가른 것과 같은 방식). 대상 = 카드에서 고른 핀이 여정에 다 안 들어감 / BTS 저녁이 공연 시각과 겹침. **지금 바로 하는 것이 아니라 방식을 생각 중.**

### 1-2. 외부호출 승인 필요 (돈 나감)
- [ ] 나이로비 #369 영상 완성판 = 씬2·3 캐시 재활용, 7씬만 재터치 (옛 621·632)

### 1-3. 사장님 콘솔·계정 작업 (내가 못 함)
- [ ] 계정 삭제 안내 페이지 제작 = 현재 Play 데이터보안 URL 이 앱 메인이라 구글 요건 미충족 (옛 576)
- [ ] Stripe 라이브 키 전환 + 라이브 모드 웹훅 신설(공식업뎃 필수) (옛 973)
- [ ] Stripe 공개키(pk_live) 관리자 등록 → 실기기 결제시트 확인 (옛 975)
- [ ] iOS 소셜로그인 클라이언트 ID 가 EAS 대시보드 환경변수에 있는지 확인(클라우드 빌드는 GitHub Secrets 를 못 봄) (옛 1117)

### 1-4. 실기기 확인 필요
- [ ] WebView 경로(`itinerary-map-html.ts`)도 지도 언어가 같은 원리인지 (로컬은 웹 경로만 실증함) (옛 940)

### 1-5. 코딩 착수 대기
- [ ] 대조 매칭에 좌표 교차검사 추가(바토 파리지앵 오탐 방지) (옛 150)
- [ ] 매칭 오병합 = 사장님 확정 방향으로 착수 (옛 1304)
- [ ] BTS 상세 섹션 체크 배지(`✓`) = `isSelected={false}` 하드코딩 = 절대 안 뜨는 죽은 코드 정리(§19) (옛 1109)

### 1-6. 조사·연구
- [ ] **영상 API = Omni 1.1 Flash 전환 조사** — 가격·하루 한도 확인. Vertex 로 가려던 이유가 하루 한도였는데 **Vertex 에도 한도가 있어 의미가 없어짐**(가격은 비슷). 우리에게 필요한 것은 한도 없는 쪽. Omni 1.1 Flash = 3~4일 전 나온 최신 API, 싸면 이쪽으로. (옛 631 대체)
- [ ] MIX 조립 20초 구간의 병목 지점 프로파일링 (옛 1043)
- [ ] 같은 장소 중복제안의 매칭단계 필터링 가능성 (옛 1305)
- [ ] 기기·OS 환경 대응 딥리서치 = 키보드 건과 무관한 잔여 조사 (옛 896)

---

## 2. 보류 (지금 안 한다)

### 2-1. 다국어(i18n) 전면 적용 — **보류**
**이유** = 전부 외부호출 = 돈이 든다. **지금은 임시방편으로 간다** = 베스트를 뽑을 때 이미 7개 언어를 가져와 놨으므로 그것으로 쓴다(외부호출 0). **앱이 활성화된 뒤에 더 할지 그때 판단한다.**

보류 항목 = C0 배선 실측(옛 919) · C1 `getLanguageInstruction` 1줄(옛 920) · C2 `summary_ko` 컬럼 유지(옛 921) · C3 `zh` 컨벤션 확인(옛 922) · D2 숏폼 나레이션(옛 926) · D3 §22 검증(옛 927) · 카드·생성기·뷰 모달 i18n(옛 932) · 영상 다국어화(옛 767)

### 2-2. Vertex AI — **접음**
하루 한도가 있어 갈 이유가 없어짐. 딸린 작업(API 활성 · 서비스계정 · 결제계정 확인, 옛 473~475) 전부 폐기. 대체 = 위 1-6 의 Omni 1.1 Flash 조사.

---

## 3. 끝난 일 (기록)

**2026-09-03 정리 시점에 완료 확인**
- 관리자 대시보드 리팩토링 = 구현·로컬 실증·BE 연결(계기판 2종 포함) 완료 (옛 98·99)
- 도시대표카드 소속오염 133행 = 132행 격리 완료 (옛 702)
- i18n 요약 = **하이브리드 = 임시방편**으로 처리(7개 언어 기확보분 사용, 외부호출 0) (옛 925)
- 라이브·여행비서 2개 기능 노출 방식 확정 (옛 1089)
- BTS 카드 9번째 터치 무반응 = 처리 완료 (옛 1110)
- 나이로비 결손 채움(후보 91행 검증 + 얇은 칸 발굴) 완료 (옛 126)
- 파리 등 결손 채움 = `fill/` 도구로 완료 (옛 151) — 2026-09-03 파리 이미지 236/237, 유료 0
- 베스트 v3 = 언어별 top20 교집합/차집합 + 식당 랜드마크 앵커 구현 완료 (옛 152)
- 지도 언어 = 한국어·영어로 바뀌는 것 확인, 이 정도면 만족 (옛 939)
- 오디오가이드 배지 3D 단추 = 98점 확인 후 확대 적용 (옛 1303)
- 스크린샷 잔여 결함 2건 개선 (옛 577)
- 엔진 v26·BTS·창고도구·관리자 수정 = 운영 반영 확인 (2026-09-03)

---

## 작업 기록 (여기서부터 날짜순으로 이어 쓴다)

### 2026-09-03 — 새 정본 시작
- 직전 정본이 1,388줄로 길어져 **안 끝난 것만** 추려 이 문서로 옮김. 옛 문서는 아카이브로 보존.
- 사장님 검토 반영 = 완료분 12건을 3장으로 내림 · i18n 8건은 보류(외부호출 비용, 임시방편 유지) · Vertex 3건은 접고 Omni 1.1 Flash 조사로 대체 · BTS 2건은 "전용 분기" 한 건으로 합침.
- 남은 일 = 결정 1 · 외부호출 1 · 콘솔 4 · 실기기 1 · 코딩 3 · 조사 4 = **14건**.

### 2026-09-04 — Stripe 라이브 전환
- 계정 = 기존 라이브 계정 `acct_1SXsKMPmhYZ4wY87`(Dbstour) 그대로. 새로 만든 것 없음.
- 근본 원인 = **DB `api_keys` 가 서버 기동 때 Replit Secrets 를 덮어씀**(`api-keys-loader.ts`). 사장님이 Secrets 를 라이브로 바꾸셔도 DB 의 옛 테스트 값이 이겼다.
- 조치(사장님 승인 후) = `api_keys` 2줄만 라이브로 교체 = `STRIPE_PUBLISHABLE_KEY`(pk_live) · `STRIPE_WEBHOOK_SECRET`(신설 웹훅의 whsec). `STRIPE_SECRET_KEY` 는 DB 에 행이 없어 Secrets 값이 그대로 쓰인다(그대로 둠).
- 웹훅 = `https://my-guide.replit.app/api/payments/webhook` 신설·Active. 이벤트 2종은 기동 시 `ensureWebhookSubscription()` 이 자동 보정.
- 배포 후 실증 = 공개키 `pk_live_…` / 운영에서 만든 결제 세션 `cs_live_…` / 서명 없음 503 · 가짜 서명 400.
- 운영 앱 화면 실증(AI 직접, §21) = 프로필 → 충전 → **실제 결제창**. 주소 `cs_live_…` · 상호 `Dbstour` · **TEST MODE 주황 배너 없음** · Apple Pay/Link 노출 · €10.00 / ₩16,300(1 EUR = 1,630 KRW) · 상품 `Tripis Credit Top-up 140 credits (100 base + 40 bonus)` · 콘솔 에러 0. 결제는 누르지 않음.
- **앱 전용 사고 = `Invalid API Key` (iOS·AOS 동일).** 원인 = 내가 공개키를 **스크린샷 눈으로 읽어 옮겨 적어** 한 글자가 틀림(`gnsg77lUP7` 의 소문자 `l` 을 대문자 `I` 로). 웹은 서버가 비밀키로 결제창을 만들어 무사했고, 앱만 이 공개키를 직접 써서 터졌다. → 사장님이 복사단추로 주신 값으로 교체 후, **스트라이프 API 에 직접 물어 유효(400) 판정** 확인. 앞으로 키는 눈으로 옮기지 않는다.
- **관통 완료(2026-09-03 21:59:20)** = 사장님 실제 카드 결제 1건 → 잔액 **558 → 698(+140)** · 장부 `purchase 140` · 참조 `pi_3UBkW4PmhYZ4wY87…`(라이브) · 최근 2시간 충전 1건/140 = **중복 없음**. 카드 → 스트라이프 → 웹훅 → 충전 전 구간 실증.
- Stripe 라이브 전환 = **완료**.

### 2026-09-04 — 충전 성공 알림에서 "Alert" 제거
- 증상 = 결제 성공인데 앱에 제목 **`Alert`** 인 경고창이 뜸(iOS·AOS 동일). 사장님 = "성공했는데 무슨 경고냐".
- 원인 = `useProfile.ts` 가 `alert()` 를 씀. RN 은 이걸 제목 `Alert` 인 경고창으로 띄운다. 언어를 한국어로 바꿔도 그 글자는 앱이 붙이는 것이라 안 없어진다.
- 조치(사장님 승인 = **성공 알림 1곳만**) = 앱은 `Alert.alert(제목, 본문)` 으로 교체 → 제목 = `충전 완료`. 7개 언어 `topupDoneTitle` 신설(짧게 축약).
- 웹은 그대로 `alert()` 유지 = **RN Web 의 `Alert.alert` 는 빈 함수**(`node_modules/react-native-web/.../Alert/index.js` 원본 확인) = 바꾸면 웹에서 알림이 아예 사라진다.
- **실패 알림 4곳은 손대지 않음**(사장님 = 실패는 경고가 맞다) = `sheetPrepareFailed` · `sheetStartFailed` · 스트라이프 실패 사유 · `topupError`.
- **문구 = 제목·본문 중복 금지(사장님 지시).** 제목만 완료를 알리고 본문은 손님께 감사 인사. `충전 완료 / 충전해 주셔서 감사합니다.` 처럼 같은 말을 두 번 하지 않는다.
- **화면 실증(§21) = 7개 언어 전수.** 새 웹빌드를 로컬 5055 로 띄우고(5000 은 옛 프로세스가 옛 번들을 주고 있어 함정 = 해시 대조로 발견) 번들을 훑어 이 키를 가진 모듈 수를 먼저 셈(7개 = 누락 불가) 후 전부 화면 대조. 폰 알림창 폭(270px)에서 **7개 전부 1줄 = 넘침 0**.

  | 언어 | 제목 | 본문 |
  |---|---|---|
  | ko | 충전 완료 | 충전해 주셔서 감사합니다. |
  | en | Top-up complete | Thank you for your top-up. |
  | ja | チャージ完了 | ありがとうございます。 |
  | fr | Rechargé OK | Merci pour votre recharge ! |
  | zh | 充值完成 | 感谢您的充值。 |
  | es | Recarga hecha | ¡Gracias por tu recarga! |
  | de | Aufgeladen | Danke für Ihre Aufladung! |

  프랑스어 = 사장님 지적으로 악센트 보정(`Rechargé`), `OK` 는 프랑스에서 그대로 쓰므로 유지. 일본어 = 넘쳐서 `ありがとうございます。` 로 축약(**넘치면 억지로 다 쓰지 말고 줄인다** = 사장님).
- 웹 분기는 **죽은 코드라 삭제**(§19) = 웹은 결제창으로 `window.location.href` 후 return 하므로 이 성공 알림에 도달하지 않는다. tsc 에러 120 → 119.
- 판단3종 = review·react-best 통과. simplify 가 잡은 1건 = 코드가 아니라 `CLAUDE.md:430` 의 옛 정본 링크 잔존(§19) → 중복 괄호 삭제로 해소.

### 2026-09-04 — 검증 워크플로 결함 수정 (react-best 가 헛돌던 것)
- 증상 = 판단3종 중 **react-best 가 "검사 대상 TSX 0건"** 으로 실패. 사장님 = "RN인데 리액트는 항상 띄워야지. 대상이 아니다를 네가 판단하면 안 되지."
- 원인 = `scripts/verify-workflow.mjs` 가 react-best 에게 **"TSX 에 대해 검토하라"** 고 시킴. 이 앱은 RN 이라 화면 동작이 `.ts` 훅에도 있는데 TSX 만 보게 해 둠. 게다가 `args.tsxChanged:false` 로 **react-best 자체를 건너뛸 수 있는 장치**까지 있었다.
- 조치 = ① 프롬프트를 "RN 앱이다. TSX 뿐 아니라 `.ts` 훅·컴포넌트 로직·i18n 문구까지 대상" 으로 교체 ② **스킵 장치 완전 삭제**(`tsxChanged` 변수·조건분기·헤더 주석, §19) ③ 조건부 `JUDGE.push` → 배열 리터럴 1벌(§0).
- 실증 = 고친 워크플로 재실행 = **3/3 통과**. react-best 가 이번에는 `.ts` 훅과 로케일 7개를 직접 열어 파싱까지 하고 판정.
- 부수 소득 = react-best 가 내 앞선 오판을 교정. "언어를 바꾸면 직전 언어로 뜰 수 있다"는 내 말은 **틀렸다** — `t` 는 버튼을 누른 뒤 호출되므로 그 시점 언어가 적용된다.
