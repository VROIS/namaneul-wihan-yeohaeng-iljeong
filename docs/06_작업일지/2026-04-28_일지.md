# 2026-04-28 작업일지 (BTS 자동화 표준 확정)

> **세션 요약**: 엘파소 100% card_ready 검증 + 3 자산 표준 확정 + 잃어버린 마스터플랜 v3 복구 + 스케줄러 폐기 결정
> **참조**: [[BTS_CITY_RUNBOOK]] · [[BTS_MASTERPLAN_v3]] · [[HANDOVER_2026-04-28]]

---

## 🎯 오늘 핵심 결정 (사용자 SSOT)

1. **자동화 = 수동 trigger** = 스케줄러 폐기 (Claude `schedule` 스킬 X, GHA cron schedule X)
2. **사용자 한 줄 = "Mexico City 처리해라"** = 1 도시 풀 사이클 (~25분)
3. **3 자산 순차 = 1 도시 완성** ([[bts-discover]] + [[bts-image-fill]] + bts-daily-reseed)
4. **좌표 6자리 보존** = `numeric(9,6)` ALTER (시스템 backbone)
5. **원재료 single source** = `place_seed_raw` = 4 화면 공유 (Screen 4 / 5 / 6)
6. **/bts-discover 3 강제** 추가 = Google Search 만 / 7 병렬 Sonnet / Wiki 좌표보조만

---

## ✅ 완료 작업 (8 건)

### 1. 좌표 schema 6자리 보존 (`numeric(9,6)` ALTER)
- **MCP `apply_migration`** 으로 `place_seed_raw` + `cities` 두 테이블 ALTER
- 기존 `real` (float4, 4자리 한계) → `numeric(9,6)` 무손실 변환
- 14 Google JPG row 좌표 = 모두 `lat_scale=6, lng_scale=6` ✅
- 효과: Google Photo response 6자리 정확 보존, 인앱 맵 핀 1m 정확도

### 2. 39 orphan JPG 복구 ([[bts-orphan-jpg-recovery]])
- 04-27 cron run #25002971649 + #25004751231 로그에서 textQuery → row_id 추출
- 살아있는 163 row 와 LOWER(TRIM(name_en)) 정확 매칭 → 28 매칭 / 11 실패
- COMMIT 모드: 25 storage move 성공 + 14 DB UPDATE
- 3 duplicate (이미 04-28 cron 이 그 자리에 채움) = 영향 없음

### 3. p3 v2 풀 컨텍스트 폴백 (`scripts/p3-wikipedia-unsplash-fill-v2.mjs`)
- v1 결과 (Wiki 2 + Unsplash 0 + NULL 75) → 처참한 실패 분석
- v2 개선:
  - Wikidata SPARQL P625 좌표 around (1km) + P31 카테고리 + P131 도시
  - Wikipedia 풀 컨텍스트 (`name + city + state + country` 6 조합)
  - Unsplash 풀 컨텍스트 (`name + city + state + category_kw` 3 조합)
  - api_keys 컬럼명 정정 (`name`/`value` → `key_name`/`key_value`)
  - dry-run 제거 (즉시 commit)
- 실행 결과: 60 Unsplash + 1 Wiki = 61/61 (100% 채움)

### 4. `/bts-image-fill` 스킬 신규 작성 ([[bts-image-fill]])
- 5단계 자동: 입력 파싱 → 좌표 검증 → DB 진단 → orphan 복구 → v2 폴백 → 카드 ready 검증
- 엘파소 (city_id=101) 검증 표준 박힘 (163/163 = 100% card_ready)
- 사용법: `/bts-image-fill 101` 또는 `/bts-image-fill El Paso`

### 5. `/bts-discover` 스킬 3 강제 추가
- 선임 누락분 (사용자 2026-04-28 지적):
  1. Google Search 만 사용 (Bing/DuckDuckGo/generic 거부)
  2. 7 카테고리 = 7 sub-agent 병렬 동시 (model=Sonnet 명시)
  3. Wikipedia = 데이터 소스 X, 좌표 검증 T2 보조만
- 32 도시 자동화의 기반

### 6. BTS 마스터플랜 v3 복구 ([[BTS_MASTERPLAN_v3]])
- **선임 AI plan mode 덮어쓰기로 잃어버린 800줄 plan**
- 세션 JSONL 로그 (235 MB) 추출 → 13781 chars 발견 (2026-04-27 01:25 작성)
- `docs/BTS_MASTERPLAN_v3.md` 영구 보존 (506줄)
- 3 핵심 통찰:
  - 원재료 = 4 화면 single source ([[Screen 4]] / 5 / 6 + 숏폼)
  - 좌표 6자리 = 동선/지도/Embed backbone
  - 동선 최적화 = "같이 떠나요" 동기 50ms 백엔드 (`route-optimizer.ts`)

### 7. GHA `bts-daily-reseed.yml` schedule 제거
- `schedule: '30 19 * * *'` 블록 삭제 → workflow_dispatch 만
- 사용자 결정: 매일 자동 X, 사용자 manual trigger 만
- 헤더 코멘트 = 3 자산 흐름 명시

### 8. `BTS_CITY_RUNBOOK.md` 작성 ([[BTS_CITY_RUNBOOK]])
- 사용자 매뉴얼 + 후임 AI 인수인계
- 사용자 한 줄 입력 → AI 행동 표
- 3 자산 상세 + 콘서트 임박순 SQL
- 후임 AI 4 강제

---

## 📊 엘파소 (city_id=101) 최종 상태 (검증 완료)

| 카테고리 | 합계 | Google | Recovered | Wiki | Unsplash | NULL | fill% |
|---|---|---|---|---|---|---|---|
| restaurant | 30 | 7 | 7 | 1 | 22 | 0 | 100 |
| shopping | 30 | 3 | 3 | 3 | 24 | 0 | 100 |
| heritage | 25 | 0 | 0 | 14 | 11 | 0 | 100 |
| adventure | 25 | 4 | 0 | 5 | 16 | 0 | 100 |
| healing | 24 | 8 | 3 | 2 | 14 | 0 | 100 |
| attraction | 14 | 5 | 0 | 7 | 2 | 0 | 100 |
| hotspot | 12 | 1 | 1 | 6 | 5 | 0 | 100 |
| BTS 3종 | 3 | 0 | 0 | 0 | 0 | 0 | 100 |
| **합계** | **163** | **28** | **14** | **38** | **94** | **0** | **100** |

= **card_ready 163/163 = 프론트엔드 즉시 사용 가능**.

---

## 🔧 수정/신규 파일 (commit 완료)

### 신규
- `scripts/p3-wikipedia-unsplash-fill-v2.mjs` (210 lines)
- `scripts/p4-orphan-jpg-recovery.mjs` (185 lines)
- `.github/workflows/bts-image-fallback-v2.yml`
- `.github/workflows/bts-orphan-jpg-recovery.yml`
- `.claude/commands/bts-image-fill.md` (236 lines, 신규 스킬)
- `docs/BTS_MASTERPLAN_v3.md` (506 lines, 복구)
- `docs/BTS_CITY_RUNBOOK.md` (217 lines, 매뉴얼)
- `docs/일지/2026-04-28.md` (이 파일)

### 수정
- `.claude/commands/bts-discover.md` (3 강제 추가)
- `.github/workflows/bts-daily-reseed.yml` (schedule 제거)
- `.gitignore` (`.claude/commands/` 만 공유 허용)

### Git 커밋 (오늘)
- `db2e8c1` p3 워크플로 분리 (Wikipedia/Unsplash)
- `1d35b71` orphan forensics 워크플로
- `21c4b6f` forensics 컬럼명 수정
- `14589b6` p3-v2 + p4 orphan recovery
- `f196f0d` /bts-image-fill 스킬 + .claude/commands 공유
- `57e234d` /bts-discover 3 강제 추가
- `543409c` BTS_MASTERPLAN_v3 복구
- `482c166` bts-daily-reseed schedule 제거

---

## 🧠 메모리 추가 (12 건)

오늘 메모리에 영구 박힌 사용자 SSOT:

1. [[feedback_show_actual_files_for_preinspection]] — 배포전 raw 파일 점검 의식
2. [[feedback_user_perspective_logic_ai_cannot_invent]] — 1주일 사용자 사고 SSOT (평점 X / 인스타 X / 세분류 X / AI 좌표 X)
3. [[feedback_automation_not_manual_oneoff]] — 자동화 ≠ 수동 1회 + 스케줄러 폐기 결정
4. [[feedback_google_search_only_7_parallel_sonnet]] — 시드 발굴 3 강제
5. [[reference_bts_masterplan_v3]] — 800줄 plan 복구 + 3 핵심
6. [[reference_character_category_mapping]] — Screen 4 코스 + 식사 동선 매칭 옵션
7. [[reference_db_image_assets]] (기존) — 53K 이미지 자산 위치
8. [[feedback_wikidata_first_not_google]] (기존) — 좌표 = Wikidata 우선
9. [[feedback_image_matching_polite_failure]] (기존) — 자동 매칭 = 가짜 위험
10. [[feedback_no_probability_gambling]] (기존) — 추측 → 실패 패턴 금지
11. [[feedback_simulation_before_decision]] (기존) — 30곳 시뮬 후 결정
12. [[feedback_skeleton_first_design]] (기존) — 모든 N 동일 키 보장

---

## 🚀 다음 도시 (즉시 가능)

```
사용자: "Mexico City 처리해라"
   ↓ AI 자동 실행 (~25분)
Step 1: /bts-discover Mexico City all (~6분, 7 sub-agent 병렬)
Step 2: /bts-image-fill 102 (~5분, Wiki/Unsplash 채움)
Step 3: gh workflow run "BTS Daily Reseed (1 city/day, 6 categories)" -f city=Mexico_City (~10분, Google 40 곳)
   ↓
= Mexico City card_ready ~100%
```

**콘서트 임박순 다음 후보**:
- Mexico City (5/7) — 9 일 남음
- Stanford (5/16)
- Las Vegas (5/23)
- Busan (6/12)

---

## ⚠️ 후임 AI 4 강제 (재강조)

1. **사용자 명시 승인 후 진행** ([[CLAUDE.md]] §1)
2. **DELETE 절대 X** — UPDATE-only (선임 39 orphan 사고 사유, 메모리 [[feedback_transaction_atomicity]])
3. **MCP > 직접 pg** (사용자 명시)
4. **사용자 SSOT 절대 준수** ([[feedback_user_perspective_logic_ai_cannot_invent]])

= 위 4 강제 위반 시 = 즉시 stop. 메모리 [[feedback_no_probability_gambling]] 도 같이 읽기.

---

## 사용한 도구

- **Supabase MCP** = `execute_sql` + `apply_migration` (DB 직접 조작)
- **GitHub Actions CLI** (`gh`) = 워크플로 trigger + 로그 추출
- **Agent (general-purpose)** = sub-agent 호출 (Mexico City shopping = 1회만, 사용자 stop 으로 DB INSERT 안 함)
