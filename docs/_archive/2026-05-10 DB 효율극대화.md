# 2026-05-10 — DB 효율 극대화 = 핵심 확정 단계 작업 기록

> **목적**: 오늘 = 메인앱 데이터 흐름 + DB 일관성 + 비용 보호 = **근원적 SSOT 확정**.
> **방법**: 모든 대화 = 시간순 + 주제별 = 누락 없이 기록 (= 사용자 명시 기록 모드).
> **수정 정책**: CLAUDE.md 제2조 준수 = 작동하는 코드 = 사용자 명시 시에만.

---

## 0. 핵심 SSOT 확정 (= 오늘 결정)

| 영역 | 확정 | 근거 |
|---|---|---|
| **메인앱 표준 row** | **`gemini3-2026-05` phase = 9 필드** = 100% 채움 | DB 검증 = 1,036 곳 = 모든 필드 87-100% |
| **`nubi_reason` 컬럼** | ❌ **폐기** (= 검증 부재 = 소설 수준) | 인프라 한계 + 사실 검증 로직 부재 |
| **신뢰성 라벨 대체** | ✅ **"구글 리뷰 N 개 (YYYY-MM-DD 기준)"** | 사용자 SSOT = RC + 신선도 |
| **`description`** = 숏폼식 (= summary_ko) | ✅ 유지 | 슬롯 + 모달 + 숏폼 영상 모두 사용 |
| **DB 매칭 표준** | name_en + name_local + **address** + **google_place_id** = 4 키 | AG3 알고리즘 일관성 |
| **운영 진입점** | `pipeline-v3.ts` (= 옛 v1) vs `orchestrator.ts` (= 데드 = v2 구현 보유) | **결정 보류** = 큰 작업 = 별도 결정 |
| **Google API 비용 보호** | "Quota cap 33 = 비용 보호" = ❌ AI 착각 = **DB 매칭 100% = 호출 0** 이 본질 | 코드 검증 |

---

## 1. 운영 현미경 검증 (= 16 슬롯 추적)

### 인프라
- 운영 도메인 = `https://my-guide.replit.app`
- DB 직접연결 = `aws-1-eu-west-3.pooler.supabase.com:6543` (= MCP 우회 = `pg` 패키지)
- 게스트 진입 = `LoginScreen.tsx:432 handleGuestBrowse` ("로그인 없이 실행" 버튼)

### 응답 (= 2026-05-09 캡처) = `scripts/_capture/2026-05-09-prod-buttons/data/itinerary-response.json`
- 16 곳 / 3 일 / 1 인 €293 / 16,340ms
- metadata = `_pipelineVersion: 'v3-2step'`, `sourceMode: 'mixed'`

### 사용자 인지 3 문제 = 정확 위치 추적
| # | 사용자 인지 | 코드 위치 | 원인 |
|---|---|---|---|
| 1 | 중복 오류 | Day 3 = 사크레쾨르 ↔ 몽마르트르 = 56m | AG2 Gemini = 별개 슬롯 생성 |
| 2 | 이미지 누락 | #8 레 코코트 = `image_url=NULL` | AG3 saveNewPlacesToDB = searchText 결과 0 |
| 3 | 웹 지도 안 열림 | `PlaceDetailModal.tsx:351 <WebView>` | react-native-webview = 웹 미지원 |

---

## 2. AG2 v1 옛 prompt 발견 (= 5 번 지시 미반영)

### git log 추적
- `pipeline-v3.ts step1 prompt 블록 (240-420)` = `cb3419f (2026-03-07)` = **2 개월 전 옛 v1**
- `ag2-gemini-recommender.ts (= v2)` = `2ed74c1 (2026-05-07)` = 5-07 v2 작성 ✅
- `itinerary-generator.ts:1994` = `runPipelineV3` 호출 = **운영 = pipeline-v3 = 옛 v1 사용**
- = **`ag2-gemini-recommender (v2)` = 데드 코드** (= orchestrator 만 호출 = 운영 미사용)

### v1 vs v2 prompt 차이 (= `docs/2026-05-08 앱 1차 제미나이 프롬프트 버젼1.md` 비교)
| 항목 | v1 (= 운영) | v2 (= 사용자 SSOT) |
|---|---|---|
| 응답 필드 | name/nameKo/nameLocal/type/startTime/endTime/reason/estimatedCostEur (= 8 필드) | name_en/name_local/**address**/**summary_ko** (= 4 필드) |
| `address` | ❌ | ✅ |
| `summary_ko` | ❌ | ✅ |
| `estimatedCostEur` | ✅ | ❌ (= AG4 가 결정) |
| 슬롯 분배 | "총 6곳" 단일 | 카테고리별 (`hotspot/heritage/healing/restaurant`) |

---

## 3. 슬롯의 정의 + 결정자 (= 사용자 SSOT)

### `server/services/agents/types.ts:113-116` = PACE_CONFIG
```ts
Packed:  { slotDurationMinutes: 90,  maxSlotsPerDay: 8 },
Normal:  { slotDurationMinutes: 120, maxSlotsPerDay: 6 },
Relaxed: { slotDurationMinutes: 150, maxSlotsPerDay: 4 },
```

### 결정 흐름
```
사용자 = vibe 3 개 + travelPace 선택
   ↓
AG1 (= Skeleton Builder)
   ├─ vibeWeights 계산 (= 100 / 60-40 / 50-30-20)
   ├─ daySlotsConfig (= 가용시간 / slotDurationMinutes)
   └─ category 슬롯 매트릭스 (= vibe → category 매핑)
   ↓
AG2 (= Gemini)
   = "이미 계산된 슬롯 수" prompt 받음 = 결정자 X
   ↓
AG3, AG4
```

### vibe → category 매핑 (= `ag2-gemini-recommender.ts:89-95` = 이미 있음)
```ts
Foodie: 'restaurant'
Healing: 'healing'
Hotspot: 'hotspot'
Adventure: 'adventure'
Romantic: 'hotspot'
Culture: 'heritage'
```

= **이미 데드 코드에 구현됨** = 새로 만들 필요 X = CLAUDE.md 제2조 (= 작동하는 코드 안 건드림).

---

## 4. DB 일관성 = 92% 누더기 (= 근원 문제)

### 전체 분포 (= 10,830 곳)
| 컬럼 | 채움률 |
|---|---|
| `name_en` | 100% |
| `image_url` | 77% (= 23% NULL = 2,510 곳) |
| `latitude/longitude` | 39% (= 61% NULL) |
| `google_review_count` | 37% |
| `google_place_id` | 35% |
| `name_ko` | 20% |
| `summary_ko` (= 숓품식) | **15%** ❌ |
| `address` | **13%** ❌ |
| `name_local` | 10% |

### **완전 row (= 9 필드 모두) = 884 / 10,830 = 8.2%** = **92% 누더기**

### phase 별 = 7 가지 표준 충돌
| phase | 곳 수 | image | place_id | address | summary_ko |
|---|---|---|---|---|---|
| **`gemini3-2026-05`** ⭐ | **1,036** | **99%** | **75%** | **100%** | **100%** |
| `bts2026` | 3,214 | 65% | 61% | 8% | 17% |
| `europe30` | 3,044 | 100% | 19% | 0% | 0% |
| `france30` | 2,129 | 100% | 3% | 0% | 0% |
| `null` | 948 | 0% | 0% | 0% | 0% |
| `google_api_reverse` | 396 | 3% | 97% | 0% | 0% |
| `auto-learn-2026-05` | 63 | 13% | 16% | 100% | 16% |

= **`gemini3-2026-05` 만 = 사용자 SSOT 표준** (= 1,036 / 10,830 = 10%).
= 나머지 90% (= 9,946 곳) = **표준 row 형식 미충족** = 보강 또는 폐기 필요.

### 도시별 = 9 발굴 도시 vs 옛 시드
| 분류 | 도시 | image% | place_id% | address% | summary% |
|---|---|---|---|---|---|
| **gemini3-2026-05** ⭐ | 파리/멕시코/런던/마드리드/스탠퍼드/부산/브뤼셀/뮌헨/라스베이거스 | 88-94% | 63-81% | 72-82% | 91-98% |
| 옛 시드 | 리옹/피렌체/베를린 | 100% | 0-21% | 0% | **0%** |

---

## 5. INSERT 경로 = 9 가지 = 표준화 부재

```
[운영 흐름]
ag3-data-matcher.ts:762       = 신규 발굴 (= AG2 → searchText → INSERT)

[시드 발굴]
mcp-raw-service.ts:917        = 옛 시드
scripts/seed-gemini.mjs:493   = ⭐ gemini3-2026-05 표준 (= 사용자 SSOT)

[데이터 보강 스크립트 = 6 종]
scripts/build-upsert-sql.mjs
scripts/consolidate-v3-best-rank.mjs
scripts/gen-insert.mjs
scripts/step1c-fix-missing-rows.mjs
scripts/step7-fill-lucide-from-raw.mjs
scripts/step7c-munich-bts-venue.mjs
scripts/step8b-rerank-with-temp.mjs
scripts/_tmp_paris-rest-insert-21new.mjs (= 어제 21 식당)
```

= 단일 표준 INSERT 함수 부재 = 사용자 SSOT 위반의 근원.

---

## 6. Google API 비용 보호 = AI 착각 vs 실제

### `server/services/google-places.ts:16-57` 코드
```ts
export const DAILY_API_LIMIT = 33;

canMakeRequest(): boolean {
  return this.count < DAILY_API_LIMIT;   // 33 도달 전 = 모두 통과
}
```

### AI 들의 보고 vs 실제
| AI 들의 보고 | 실제 |
|---|---|
| "DAILY_API_LIMIT 33 = 비용 보호" | ⚠️ **폭주 방지일 뿐** |
| "Quota cap = 안전" | ⚠️ **Quota 한도까지 청구** |
| "월 비용 = 0" | ❌ **월 ~$20 발생 가능** |

### 비용 시뮬 (= 매일 33 회 도달 시)
| API | 단가 | 일별 | 월별 |
|---|---|---|---|
| searchText (Pro) | $0.032 | $1.06 | **$31.7/월** |
| Place Details (Pro) | $0.017 | $0.56 | $16.8/월 |
| PhotoMedia | $0.007 | $0.23 | $6.9/월 |

### 진짜 비용 보호 = **호출 자체를 안 함** = DB 매칭 100%
- 운영 16 곳 = DB 매칭 13 + 신규 호출 3 = $0.117/호출
- 매칭 100% 시 = 신규 0 = **$0/호출**

### 기존 GitHub Actions Cron 상태 (= 안전)
- `.github/workflows/` 13 개 = 모두 manual trigger only (= 2026-04-28 사용자 결정)
- = 자동 호출 = **0** ✅

---

## 7. 프론트엔드 필요 필드 = 9 + 1 폐기 = 9 확정

### 슬롯 카드 (`TripPlannerScreen.tsx`)
- `image` / `name` / `nameKo` / `nameLocal` / `startTime` / `endTime` / `priceEstimate` / `userRatingCount` / `description` / `googleMapsUrl` / `lat/lng` / `isMealSlot` / `mealType`

### 모달 (`PlaceDetailModal.tsx`)
- 위 + `image hero` / `lat/lng map embed` / `nubiReasonSource`

### 숏폼 영상 (`apply-shortform-paris.mjs`)
- **`description`** = summary_ko = 한 줄 ~50자 사용자 톤
- 예: "파리 사람들이 진짜 쉬러 오는 곳. 그 유명한 초록색 의자 끌어다 햇살 아래 책 한 권 = 파리지앵 라이프 그 자체."

### `gemini3-2026-05` 표준 9 필드 = 매핑
| `place_seed_raw` 컬럼 | 슬롯 카드 | 모달 | 숏폼 | 매칭 키 |
|---|---|---|---|---|
| `name_en` | ✅ | ✅ | - | ✅ |
| `name_ko` | ✅ | ✅ | - | - |
| `name_local` | ✅ | ✅ | - | ✅ |
| `address` | (= 숙소) | - | - | ✅ |
| `latitude/longitude` | ✅ | ✅ | - | ✅ |
| `google_place_id` | - | - | - | ⭐ 100% 정확 |
| `image_url` | ✅ | ✅ | (= 영상 background) | - |
| `google_review_count` + `created_at` (= 신선도) | ✅ "rc N (YYYY-MM-DD 기준)" | ✅ | - | - |
| `summary_ko` | ✅ | ✅ | ✅ | - |

= **9 필드 = 슬롯 + 모달 + 숏폼 = 모두 매핑** = 충분.

### Atmosphere SKU = 폭탄 = 이미 차단
- 코드 fieldMask = `id,displayName,location,photos.name,googleMapsUri,userRatingCount` = **Pro SKU 만**
- Atmosphere 필드 (= 33 종 = $0.045+/req) = **0** ✅

---

## 8. `nubi_reason` 폐기 결정 (= 사용자 SSOT)

### 폐기 이유
- 기획 단계 = 차별화 라벨 의도
- 실제 = **인프라 + 로우데이터 수집 한계 + 사실 검증 로직 부재**
- = 거의 사용 못하는 **소설 수준** (= "데이터 수집 중" 4 곳 / 잘못된 패키지투어 매핑 등)
- = 신뢰성 X

### 대체 = RC + 기준 날짜
- 형식 = **"구글 리뷰 N 개 (2026-05-10 기준)"**
- 신뢰성 = ✅ Google 검증 + 신선도 명시
- source = `place.userRatingCount` + `place.__seedDirectMatch.createdAt`

### 폐기 영향 = 5 군데
| 위치 | 영향 |
|---|---|
| `pipeline-v3.ts:711-717` | 호출 라인 제거 (= 7 라인) |
| `pipeline-v3.ts:1185-1244` | 함수 통째 폐기 (= 60 라인) |
| `TripPlannerScreen.tsx:1830-1853` | 슬롯 카드 ⭐ 영역 = RC + 날짜로 대체 |
| `PlaceDetailModal.tsx:78-79 + 240-280` | displayReason + NUBI 카드 = 단순화 |
| 응답 매핑 | `lastReviewSyncDate` 1 줄 추가 |

= 약 120 라인 변경 = 사용자 명시 후 진행.

---

## 9. 진입점 결정 (= 보류)

| 옵션 | 장점 | 단점 |
|---|---|---|
| A) 진입점 = `orchestrator` 변경 | 사용자 SSOT (= v2 prompt + 매트릭스) 즉시 적용 / 1 라인 변경 | 어제 945170e 의 sourceMode = 별도 추가 (= 2 줄) |
| B) `pipeline-v3` step1 prompt = v2 로 교체 | 작동하는 진입점 유지 | 큰 변경 = 200+ 라인 = step1 + wrapper |
| C) 그대로 유지 | 변경 0 | 사용자 SSOT 영구 미적용 |

= **결정 보류** = 핵심 SSOT (= DB 표준화 + 매칭 알고리즘) 가 = 더 근원 = 우선.

---

## 10. 작업 정책 (= CLAUDE.md 제2조 준수)

### 오늘 변경된 파일 = 미커밋 (= 사용자 검토 필요)
| 파일 | 변경 | 종류 |
|---|---|---|
| `client/components/PlaceDetailModal.tsx` | (1) Image → expo-image (2) description 별도 영역 (3) web iframe 분기 | UI |
| `client/screens/TripPlannerScreen.tsx` | (1) Image → expo-image (2) description 우선 | UI |

### 롤백된 시도 (= 잘못)
- `pipeline-v3.ts haversineTransit (5분 → 1분)` = 백엔드 = 작동 코드 = 롤백 ✅
- `pipeline-v3.ts nubiReason userRatingCount 보강` = 백엔드 = 롤백 ✅
- `client/components/Icon.tsx 9 LUCIDE 추가` = 글라스 미니멀리즘 SSOT 위반 = 롤백 ✅
- `pipeline-v3.ts calculateCategoryMatrix` 함수 신규 추가 = 이미 있는 코드 무시 = 롤백 ✅

---

## 11. 다음 결정 후보

### P0 = 사용자 명시 시 진행
1. `nubi_reason` 폐기 (= 5 군데 변경)
2. RC + 날짜 표시 (= UI + 응답 매핑)
3. `gemini3-2026-05` 표준 row 적용 (= 9 INSERT 경로 → 단일 표준)

### P1 = 별도 결정
4. 진입점 변경 (= orchestrator vs pipeline-v3 step1 v2 교체)
5. 9,946 곳 = 표준 row 형식 보강 또는 폐기

### P2 = 검토
6. AG3 매칭 알고리즘 81% → 100% (= name_en + place_id + address 4 키 일관성)
7. 운영 미커밋 2 파일 (= UI = expo-image + description) 커밋 여부

---

## 12. 산출물 위치

```
scripts/_capture/2026-05-09-prod-buttons/
├── Phase-A/00-initial-load.png
├── Phase-B/B0~B4-*.png (= 5 탭)
├── Phase-C/C-form-all-filled.jpeg
├── Phase-E/E1-louvre-modal-open.jpeg
└── data/
    ├── healthcheck.json
    ├── db-baseline-PRE.json
    ├── itinerary-response.json (= 50 KB 응답 통째)
    ├── ag2-prompt-final.txt (= 운영 v1 prompt 1,949자)
    ├── ag2-raw-reconstructed.json (= 응답 역추적)
    ├── ag3-transform-diff.json
    └── db-match-table.json

scripts/
├── _diag-db-ping.mjs (= DB 연결 검증)
├── _diag-paris-baseline.mjs (= Paris 237 곳)
├── _diag-paris-check.mjs (= city 매칭)
├── _diag-cols.mjs (= 컬럼 39 종)
├── _diag-pixel-trace.mjs (= 16 슬롯 ↔ DB 1:1)
├── _diag-3-unmatched.mjs (= 르 푸무아/셰 드니즈/낭만주의)
├── _diag-extract-real-prompt.mjs (= prompt 빌드)
├── _diag-ag2-reverse-extract.mjs (= AG2 raw 역추적)
├── _diag-photo-urls-status.mjs (= photo_urls 영향)
└── _diag-db-consistency.mjs (= DB 일관성 분포)

docs/
├── 2026-05-09 운영 버튼 전수 현미경 검증.md (= 표면 보고)
├── 2026-05-09 운영 백엔드 데이터 흐름 추적.md (= 깊이 보고)
└── 2026-05-10 DB 효율극대화.md (= 본 문서 = 핵심 SSOT)
```

---

**기록 시작**: 2026-05-10
**기록 모드**: 사용자 명시 = 모든 대화 = 시간순 + 주제별 누락 없이.
**다음 추가**: 사용자 결정 + 추가 조사 + 진행 작업 = 본 파일 하단에 누적.

---

## 13. 2026-05-12 = DB 90 컬럼 분석 + DROP 43 완료

### 발견 = 컬럼 분포
- 100% 채움 = **8 컬럼** (= 필수 메타)
- 50-99% 활용 = 5 컬럼
- 10-49% 부분 활용 = 26 컬럼
- 1-9% 거의 빈 = 8 컬럼
- 🔴 **0-실제 1-34 row** = **43 컬럼** (= 데드)

### Place Details (New) SKU 5 등급 정확 분류 (= 한국어 공식)
- Essentials IDs Only: id, photos, attributions
- Essentials: formattedAddress, location, types, viewport
- Pro ($17/1,000): displayName, businessStatus, googleMapsUri, primaryType
- **Enterprise ($25/1,000)**: `userRatingCount`, rating, priceLevel, regularOpeningHours, websiteUri, internationalPhoneNumber
- Enterprise + Atmosphere ($40+/1,000): 33 종 폭탄

### Google fieldMask = "highest SKU applicable" 룰
- 한 필드라도 Enterprise = 전체 호출 = Enterprise 청구
- 같은 등급 내 = 1 개 받든 = 11 개 받든 = 동일 비용
- = 우리 SSOT = 9 필드만 (= 과유불급 회피)

### Google Maps Embed API 발견
- 비용 = **완전 무료** + **무제한**
- `place_id` 로 iframe 표시 = 모든 정보 자동 (= 사진/평점/리뷰/메뉴/시간)
- = **모달 = 별도 구현 X = Google Embed 통째 사용** = 사용자 SSOT 본질

### 14 컬럼 ↔ 기존 매핑 (= 신규 추가 0)
- 모든 14 필드 = `place_seed_raw` 기존 컬럼에 매핑 가능
- `summary_ko` 도 이미 존재
- 신규 컬럼 추가 0 = 사용자 SSOT 충족

### 실행 = DROP 43 컬럼 완료 (= 2026-05-12)
- 백업: `backups/place_seed_raw_pre_drop43_2026-05-12.json` (= 545 row 데이터, 59.2 KB)
- 트랜잭션: BEGIN → DROP 43 → COMMIT ✅
- 컬럼: 90 → **47** (-48%)
- row: 10,836 → 10,836 (= 변화 0)

#### 삭제 43 컬럼 = 3 그룹
1. Atmosphere SKU 폭탄 (= 28): delivery, dine_in, takeout, curbside_pickup, reservable, serves_beer/wine/breakfast/brunch/lunch/dinner/vegetarian_food/coffee/dessert, good_for_children/groups/watching_sports, live_music, outdoor_seating, restroom, menu_for_children, allows_dogs, accessibility_options, parking_options, payment_options, instagram_photo_urls/hashtags/post_count
2. 옛 기획 미구현 (= 10): celeb_mention, names_i18n, type, cuisine_type, cuisine_origin_country, short_address, display_name_ko, aliases, price_fetched_at, last_data_sync
3. 운영 미사용 (= 5): price_level, business_status, website_uri, phone_number, google_maps_uri

### 다음 = 추가 정리 후보
- shared/schema.ts = 43 컬럼 drizzle 정의 제거 (= 사용자 명시 시)
- sync-google-place-id-service.ts:280 = celebMention 1 라인 제거
- 나머지 컬럼 정리 = 1-9% 채움 (= 8 컬럼) + 옛 자산 (= 일부) = 사용자 결정

---

## 14. 2026-05-12 = 미커밋 변경 9 종 + 충돌 기록 (= 컴팩팅 대비)

### 14.1 사용자 SSOT 본질 (= 오늘 확정)

1. **DB 자동 캐싱** = 사용자 매 접속 → AG3 DB 매칭 X 시 → **자동 호출 + DB INSERT** → 다음부터 DB 매칭 → 호출 ↓
2. **PD 바로 호출** = searchText 폐기 = Place Details (14 요소) 직접
3. **AG2 v2 prompt** = Gemini googleSearch grounding 활성 = place_id 환각 0 (= 메모리 SSOT)
4. **모달 = Google Embed iframe** = 우리 디자인 폐기 = Google 자동 = 사진/평점/리뷰
5. **WK 이미지 = BTS 1주일 SSOT** = UA 헤더 + bucket 변환 + Platform 분기 (= 단순 expo-image 부족)
6. **DB 14 필드 표준** = `name_en, name_ko, name_local, address, lat, lng, google_place_id, image_url, google_review_count, summary_ko, price_eur, seed_category, rank, created_at`
7. **신규 컬럼 자제** = 기존 90 컬럼 → 47 → 더 축소 가능
8. **Atmosphere SKU 거부** = $40+ 폭탄 = `reviews/editorialSummary/delivery` 등 27 종 차단
9. **Enterprise SKU = 시드 발굴 1 회만** = `userRatingCount` (= rc) 받기 = $25/1,000 = 영구 자산

### 14.2 오늘 미커밋 변경 9 종

| # | 작업 | 파일 | 상태 |
|---|---|---|---|
| 1 | DB DROP 43 컬럼 | DB | ✅ COMMIT (= 트랜잭션) |
| 2 | resolvePrice 우선순위 + MEAL_BUDGET 폴백 | pipeline-v3.ts:72-99 | ✅ |
| 3 | resolvePrice 호출자 = mealType + travelStyle 전달 | pipeline-v3.ts:683-697 | ✅ |
| 4 | **PlaceDetailModal 통째 재작성 = Google Embed iframe** + `/api/bts/map-config` 키 fetch | PlaceDetailModal.tsx | ✅ |
| 5 | saveNewPlacesToDB 호출 = **복원** (= 사용자 SSOT = DB 자동 캐싱) | pipeline-v3.ts:745 | ✅ (= 5-12 폐기 → 복원) |
| 6 | runBackfillGooglePlaceId 폐기 (= priceLevel Enterprise) | sync-place-seed-trucks.ts:86-99 | ✅ |
| 7 | /api/sync/city/.../places 410 Deprecated | routes.ts:179-188 | ✅ |
| 8 | /api/admin/sync/places/... 410 Deprecated | admin-routes.ts:1353-1361 | ✅ |
| 9 | **WK 이미지 helper + 슬롯 카드 적용** = BTS 1주일 SSOT | wikimedia-image.ts + TripPlannerScreen.tsx | ✅ |

### 14.3 충돌 + 미해결 (= 사용자 정리 대기)

#### 충돌 1: searchText 완전 폐기 불가
- 사용자 SSOT = "searchText 모두 삭제" + "PD 바로 호출"
- 현실: AG2 v1 (= pipeline-v3 step1) = JSON mode = grounding 없음 = **place_id 미수신**
- 결과: AG3 saveNewPlacesToDB = searchText fallback 필요 (= place_id 없으면 PD 호출 불가)
- 해결: AG2 v2 grounding 활성 = pipeline-v3 step1 통째 재작성 (= JSON mode 폐기 + tools 추가)

#### 충돌 2: pipeline-v3 vs orchestrator
- pipeline-v3.ts (= 운영 진입점) = step1 + step2 통합 (= 1,488 라인)
- orchestrator.ts (= 데드 코드) = 4+1 분리 (= ag1/ag2/ag3/ag4)
- `ag2-gemini-recommender.ts` (= 데드 = 5-07 작성) = v2 prompt + place_id + matrix 보유
- 사용자 SSOT = 진입점 결정 = 보류 (= "스파게티 위험")

#### 충돌 3: BTS 1주일 SSOT vs 메인앱 적용 충돌
- `BTSPlaceCartScreen.tsx` 안에 = 자체 정의 (= 수정금지 주석)
- 메인앱 = `client/lib/wikimedia-image.ts` = 별도 헬퍼 추출 (= 같은 로직)
- 향후 = BTS 도 = 공통 헬퍼 사용 = SSOT 통합 = 보류

#### 미해결 4: 모달 = 우리 콘텐츠 활용 X 우려
- Google Embed iframe = 모든 정보 자동 = 우리 디자인 X
- 사용자 SSOT = "슬롯 카드 = 후킹 = 모달 클릭 = Google 정확" = 인간 본능
- 의문: 우리 description + summary_ko = 슬롯에만 = 모달 = Google 자동 = OK

#### 미해결 5: 추가 컬럼 폐기 (= 1-9% 채움 8 컬럼)
- 옛 자산 = 미사용 = 폐기 권장
- 그러나 = 보존 가치 검증 = 사용자 결정 대기

### 14.4 사용자 정리 대기 영역 (= "곧 정리해줄게")

1. **전체 파이프라인** = pipeline-v3 vs orchestrator 결정
2. **AG 별 업무 분담** = AG1 (매트릭스) / AG2 (Gemini) / AG3 (DB 매칭) / AG4 (최종)
3. **WF 흐름** = 시드 발굴 / 운영 신규 발굴 / 운영 DB 매칭 = 각 단계 명시
4. **새 자동화 스킬** = 3 종 (= 시드 도시 / 식당 / 메인앱 신규)

### 14.5 산출물 위치

```
backups/
├── place_seed_raw_pre_drop43_2026-05-12.json (= 545 row, 59.2 KB)
└── RESTORE_INSTRUCTIONS.md

scripts/
├── _diag-column-usage.mjs (= 90 컬럼 분류)
├── _diag-43-cols-code-check.mjs (= 코드 사용 검증)
├── backup-place-seed-raw-43-cols.mjs (= 백업)
└── migrate-drop-43-dead-cols.mjs (= DROP 실행)

client/lib/
└── wikimedia-image.ts (= BTS 1주일 SSOT 헬퍼)

client/components/
└── PlaceDetailModal.tsx (= 통째 재작성)

client/screens/
└── TripPlannerScreen.tsx (= 슬롯 카드 WK helper 적용)

server/services/agents/
└── pipeline-v3.ts (= resolvePrice + saveNewPlacesToDB 복원)

server/services/
├── sync-place-seed-trucks.ts (= 폐기)
└── sync-google-place-id-service.ts (= line 116 priceLevel = Enterprise 잠재 폐기)

server/
├── routes.ts (= /api/sync/city/.../places 410)
└── admin-routes.ts (= /api/admin/sync/places/... 410)
```

---

**컴팩팅 대비 = 위 §14 = 모든 진행 핵심 누락 없이 기록**
**다음 = 사용자 SSOT 정리 받기 → 전체 파이프라인 재설계**

