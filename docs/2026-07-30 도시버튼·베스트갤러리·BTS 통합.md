# 도시버튼 · 베스트갤러리 · BTS 동적화 · 메인생성기 통합

> 2026-07-30 사장님 발주·**승인 완료**. **1회성 계획 아님 — 여러 세션에 걸친 정식 실행문서.** 이 파일이 정본(SSOT).
> **목표** = 거의 완성된 앱 형태. **방법** = 단계별 + **구현 후 입증된 것만 적용**(Ralph-loop). 워크트리에서 입증되면 main 갈아끼움.
> 작업 장소 = 워크트리 `refactor-profile-layout-design` / 상태: 🚧 진행 중 / **커밋 = 사장님 지시 시에만(§10)**

---

## 🔖 다음 세션 인수인계 (2026-08-01 기준)

**커밋 상태**: main `cf6cbce` · 미커밋 0건 · 원격 동기화 완료.

| 단계 | 상태 |
|---|---|
| A(도시버튼) · C(지구본) · F(인증) · G(iOS빌드) | ✅ 완료 |
| **D(BTS 여정 생성)** | ✅ **완료** — 아래 D절 as-built + 사장님 검수 4건 해소 |
| B(베스트 갤러리) | ⏳ **다음 후보** — 영상 저장소 결정이 선행(아래 참조) |
| D10(BTS 여정 저장) · E1(하단탭 BTS 켜짐) | ⏳ 미착수 |

**폰 반영 상태**: EAS 무선 업데이트 발행됨(runtime 1.0.2, commit `b53b14c` 시점).
그 뒤 커밋 2건(`5804228`·`cf6cbce`)은 **아직 무선 발송 안 함** = 사장님이 시키실 때만 보낸다.
⚠️ **서버가 바뀌었으므로 Replit Republish 가 먼저**(안 하면 새 화면이 옛 서버에 물어봐 카드가 안 갈림).

### ⚠️ 다음 세션이 반드시 알아야 할 것

1. **배포 흐름에 끼어들지 마라** — 사장님 흐름(깃 푸시 → Replit pull → 그쪽 AI 빌드 → 웹·Expo Go·아이폰12)은 완결돼 있다.
   AI 담당 = **TF·APK 둘뿐**. 판단 = `node scripts/needs-rebuild.mjs`. 무선 발송은 **시키실 때만**.
   (2026-08-01 = 이걸 어기고 만든 `/deploy` 스킬 폐기 §19. 메모리 [[feedback_dont_invent_deploy_path_replit_flow]])
2. **Expo Go 에서 소셜 로그인 안 되는 것 = 정상**(네이티브 부품이라 껍데기에만 있음). 확인은 TF·APK 로.
3. **DB 조회 자체가 Egress 를 쓴다** — 지난달 경고의 원인이 MCP + 시드발굴 흔적이었다.
   `SUM(length(...))` 같은 전수 스캔 금지, `LIMIT` 걸고 필요한 컬럼만. Supabase MCP 는 계속 금지.
4. **커밋 전 7단계 검증은 의무**(§22). 2026-08-01 에 판단 3종을 건너뛰고 커밋했다가 실제 결함이 나왔다
   (기준 2벌 드리프트 → `cf6cbce` 로 수정). **기계 4종만 통과했다고 커밋하지 마라.**

### 💾 저장소 현황 (2026-08-01 실측) — B단계 착수 전 결정 필요

| 항목 | 실측 | 한도 |
|---|---|---|
| DB | **249MB** (그중 33MB = `guides` 표본 149행의 base64 이미지, **미배선·안 불림**) | 500MB |
| **Storage** | **0.88GB** = 사진 666MB + **영상 191MB(9개·€90 자산)** + raw 40MB | **1GB** |

- **"Grace period is over" 배너 = 초과 아님.** 무료 플랜 공지이고 청구서 13장 전부 $0.00·Spend cap 켜짐.
- **영상 1개 = 9씬 54초 = 30~38MB(최근 것) · 원가 €10.** 남은 120MB = **영상 3개분**.
- **베스트 갤러리 15개 = 570MB 필요** → **무료 한도로는 불가. 영상 만들기 전에 저장소를 정해야 한다.**
- 선택지(2026-08-01 조사): **Cloudflare R2 = 전송 무료**(영상 주력이면 최선) / Supabase Pro $25 = 250GB /
  Replit App Storage = 저장 $0.03·**전송 $0.10 per GiB**(8월 1일 인하 발표됐으나 **인하폭 미공개**).
- 🔴 **Replit 로 DB·Storage 를 옮기면 AI 가 직접 조회·조작을 못 한다**(외부 접속 경로 없음) = 오늘 같은 DB 실측 진단 불가.
  **DB 는 절대 옮기지 말 것.** R2 는 S3 API 라 AI 접근 가능.

---

## 0. 왜

결제·크레딧 백엔드 + 프로필 FE = 검증 완료. 사장님 로컬 5055 확인 → **"90% SSOT 대로 구현됨"**.

이제 **후킹 지점**. 사장님 SSOT:
> 메인앱 유입 → 즉시 BTS 버튼으로 **다른 고객 경험**을 주는 **새 인터페이스일 뿐. 모든 BE+DB 공유.** 껍데기와 사용자 입력만 아미·MZ 식.

= **생성기·동선·인증은 메인앱 1벌.** 그리고 **하단 5단 버튼 = 마케팅 특화** — 여정 화면이 아닌 곳에서도 여정을 생성하게 만드는 우리 앱의 차별점이라 메인앱과 **동일하게** 구현하고 어디서나 고정.

---

## 1. 실측 (재조사 불필요)

### 1-1. 완비 도시 = 사장님 "5개" 와 일치
파리 761 · 마드리드 728 · 뮌헨 466 · 런던 451 · 브뤼셀 425 → **≥300 = 5개** (120↑ 44 / 1↑ 112 / 전체 120)
- 판정 코드 **이미 있음** = `ag2-gemini-recommender.ts:32` `READY_THRESHOLD=300` + `isCityReady()` (사장님 SSOT 2026-07-05) → **이걸 씀(§16)**
- 화면은 `InputStep.tsx:41-58` **16개 하드코딩**(커밋 `4516028`), 11개 미완비, 미리보기 5개뿐 → `RepresentativeTripShortForm.tsx:104` `|| CITY_PREVIEW_MAP.Paris` 로 **11개가 전부 파리 카드**
- `GET /api/cities` = 120개 무필터 + 클라 호출 0

### 1-2. 대표여정 표시 방법 확보
`itineraries.status` 실제 값 = `saved`(64)·`inquiry`(6)·`completed`(1) → **`status='representative'` 는 미사용 = 스키마 변경 0**. `user_id NOT NULL` → 사장님 계정 보유가 맞음. 현재 DB 여정 = **파리 1건, 영상 0건**. `CITY_PREVIEW_MAP` 은 전부 손으로 쓴 글 + 유니스플래시 링크, `fetch` 0건, **"대표 숏폼" 배지는 장식**.

### 1-3. 🎯 BTS = DB 에 이미 다 있음 (핵심)
| 항목 | 실측 |
|---|---|
| BTS 도시 | **34개**, 전부 `bts_concert_dates` + `bts_verified` |
| **공연장 좌표** | **`seed_category='bts_venue'` 32개** (예: Tampa `Raymond James Stadium` 27.9758,−82.5033) → ✅ **왕복 기준점 확보** |
| **공연 시각** | `bts_show_times` = `[{date,time}]` (19:00·20:00 등) → ✅ **여정 종료를 공연 시작에 맞출 수 있음** |
| 아미 전용 | **`bts_merch_store` 32 · `bts_army_zone` 32** 이미 발굴됨 |
| 일반 장소 | 도시당 46~131개 |
| 🔴 `bts_archived` | **2개만 true** = 낡음. 오늘(7/30) 기준 지난 공연은 파리(7/17)까지 **13개** |
| 남은 공연(날짜) | **21개** (이스트러더퍼드 8/1 ~ 마닐라 2027/3) |

→ **"남은 공연" = `bts_archived` 아니라 `공연날짜 최댓값 ≥ 오늘`**

### 1-4. BTS 화면 = 목업 경계
`InputStep`💜 → `BTSLanding`(아미봉) → `BTSWorldMap`(지구본) → `CharacterSelect` → `PlaceCart`(8장) → **"같이 떠나요"** → `BTSLoading` → `BTSDashboard`

| 화면 | 판정 |
|---|---|
| BTSLanding | 진짜 API (+`2026-04-09` 폴백 = 지난 날짜라 거짓) |
| **BTSWorldMap** | 🔴 좌표 13개 하드코딩 / **웹 iframe 은 D-Day·날짜·경기장까지 하드코딩** |
| CharacterSelect | 정상. 단 `MEMBER_NAMES` 가 서버와 **2벌** |
| **PlaceCart(8장)** | ✅ **진짜 DB** |
| **BTSLoading** | 🔴 **사장님 결정 = 삭제**(불필요). 가짜 메시지·인위적 3초 지연 전부 사라짐 |
| **BTSDashboard** | 🔴 예상시간 `places.length*1.5시간`(서버 실제 시각 무시) / `ReelCard` 가 **서버가 준 사진을 버림** / **저장 안 됨** |

### 1-5. 생성기 2벌 (§16 위반)
`/api/bts/generate`(`bts-routes.ts:392`) = 좌표를 뽑아놓고 **동선에 안 넘김** → 응답에 **`lat:0,lng:0`** / 실패 시 **고정 시간표** / 항상 1일 / **무인증·무차감** / 저장 안 됨.

### 1-6. 🔑 "고른 곳 고정"·"공연장 왕복"·"식사 자리" = 사장님 지적이 다 맞았음
| 사장님 모델 | 실측 |
|---|---|
| "최대 8곳만 PSR 에서 가져와 채움" | ✅ 정확. **BTS 에 이미 그 코드 있음** — `bts-routes.ts:420-450` `slice(0,8)`+`inArray` |
| "항상 공연장 출발↔도착 왕복" | ✅ 가능. 기준점(`route-local.ts:295-307`)이 `숙소→도시중심→목적지` 순으로 좌표를 받음 → 그 자리에 **`bts_venue` 좌표**. 폐루프 Held-Karp 가 **이미 왕복** |
| "식당 자리는 이미 고정 구현돼 있음" | ✅ 정확. **점심 = 슬롯3 고정**(`route-local.ts:396` `lunchIdx`), **저녁 = 마지막 슬롯 고정**(`:410`). 어느 식당을 넣을지만 자동 선택 → **고른 식당을 그 고정 자리에 넣으면 끝**(제가 "한계"라 한 것 철회) |

**최소 변경 = 2군데:** ① `types.ts` `TripFormData` += `pinnedPlaceIds?: number[]`(spread 가 이미 통과 = 배관 0) ② `fetchFromPlaceSeedRaw` 에 `inArray` 조회 + **`rank` 를 −1 로 덮기**(안 덮으면 `route-local.ts:321-324` 의 `rank ASC` + `.slice()` 에서 **조용히 탈락**). AG1·AG4·동선은 한 줄도 안 건드림.

### 1-7. 하단 5단
`MainAppBottomTabBar.tsx` 붙은 곳 3개(CharacterSelect·PlaceCart·Dashboard). **BTSLoading 은 삭제되므로 누락 해소.** 예외 2개(지구본·아미봉) **이미 올바르게 제외 ✓**
결함: ① `activeTab="BTS"` 가 tabs 키에 없어 **아무것도 안 켜짐** ② **AI의견 탭 영구 비활성**(`!currentItinerary`) ③ `useNavigation` 타입 부정확(런타임 정상)

### 1-8. 인증 실측 = 사장님 지시가 정본, 실제 JSX 가 어긋남
| | 순서 |
|---|---|
| 코드 SSOT 주석 (`LoginSheet.tsx:83`) | 생년월일 → **구글 → 카톡** → 이메일 |
| **실제 JSX** | 생년월일 → **카카오(`:211`) → 구글(`:231`)** → 이메일 ← **주석과 반대 = 버그** |
| **사장님 지시(정본)** | **생년월일 → 구글 → 카톡 → 애플 → 메일** |
| 애플 | `LoginSheet.tsx:5` = *"애플 = 별도 세션"* → **미구현 확정** |

- 🔴 **애플 로그인 = 앱 전체에 없음** — 서버 라우트 0 / `auth-apple.*` 없음 / `expo-apple-authentication` **미설치** / `UserData.provider` = `"kakao"|"google"|"whatsapp"`. 단 `.env` 에 애플 키 **4개는 있음**.
- **기기별 분기 = 내손앱 방식** — 애플 버튼은 **iOS 만 노출, 안드로이드는 비노출. 단 공간은 확보**(레이아웃 튐 방지).
- BTS 랜딩은 진짜 인증을 import 해놓고 `handleLogin` 이 **바이패스**(→ 바로 `goToWorldMap()`) = **버튼 눌러도 로그인 0.** 워크트리가 바이패스를 더 넓힘(웹 `isDisabled=false` + `⚡체험용` 버튼). `ArmyBombAuth*` = `setTimeout(2000)` 가짜 + 미연결. `/api/bts/*` = 인증·차감 0.

---

## 2. 사장님 결정 (2026-07-30, 구속력)

1. **도시 버튼 = 서버가 DB 실측으로 동적 노출**(≥300행, 완비순). 지금 5개 → 발굴하면 자동 증가
2. **대표여정 = 사장님이 직접 선정해 올리는 "베스트 갤러리."** AI 임의 생성 금지. **저는 올리는 장치만**
3. **미리보기 레이아웃 = 사장님 결정.** 저는 **가짜 데이터를 진짜로 바꾸는 것만**
4. **BTS 지구본 = 남은 공연도시 DB 동적화.** 8장 화면 상단 도시명과 **같은 소스 연동**
5. **BTS 생성기 = 메인앱 1벌.** 왕복 = **항상 공연장 출발↔도착**
6. **BTS 는 메인앱보다 단순 = 1인 · 1일.** **비용 필드는 별 소용 없음** — 어차피 장소와 위치를 보여주는 것이 목적
7. **`BTSLoading` 화면 = 삭제**(불필요)
8. **`ReelCard` 이미지 = 살림.** 지금은 위키 사진이 많지만 발굴하며 구글 이미지로 교체될 것이므로 **표시를 유지**
9. **AI의견 탭 = BTS 에서도 활성화.** 어차피 크레딧 차감됨. 백엔드 = **메인앱과 같은 엔드포인트·같은 프롬프트, 화면 맥락(헤더)만 다르게**
10. **인증 = 껍데기만 다른 새 창, 요소 동일.** 순서 = **생년월일 → 구글 → 카톡 → 애플 → 메일.** 애플 = **iOS 노출 / AOS 비노출, 공간은 확보**
11. **하단 5단 = 메인앱과 동일 구현 + 어디서나 고정**(예외 = 지구본·아미봉). 마케팅 특화
12. **번들ID = `com.sonanie.guide`**(내손앱은 다른 기기에서 볼 수 있고 **덮어도 무관**). **iOS 빌드도 제가 함**
13. 결제 예외처리(환불·경합) = 상용화 전이므로 미룸

---

## 3. 작업 리스트 (Ralph-loop = 묶음별 구현 → 입증 → 적용)

### A. 도시 버튼 = DB 실측 동적화
| # | 할 일 | 파일 |
|---|---|---|
| A1 | `GET /api/cities/ready` — 비BTS 행수 ≥ `READY_THRESHOLD(300)` 을 **행수 많은 순**. 상수는 `ag2` 에서 **import**. 본보기 = `/api/bts/cities` | `server/city-place-routes.ts` |
| A2 | 하드코딩 16개 **완전삭제**(§19) → A1 결과. 로딩 중엔 칩 줄 비움 | `InputStep.tsx:41-58, 206-241` |
| A3 | `|| CITY_PREVIEW_MAP.Paris` 폴백 **삭제**(§0) | `RepresentativeTripShortForm.tsx:104` |

**입증**: 칩 **정확히 5개**, 도쿄·로마 등 사라짐, API=화면 일치

### B. 베스트 갤러리 (레이아웃은 사장님 것 그대로)
| # | 할 일 |
|---|---|
| B1 | **"대표로 올리기" 장치** — 사장님 계정 여정을 `status='representative'`. 도시당 1개(새로 올리면 옛것은 `saved`) |
| B2 | `GET /api/cities/:id/representative` (없으면 204) |
| B3 | 손으로 쓴 글·유니스플래시 링크 **완전삭제**(§19) → 진짜 여정의 **장소명·DB 사진·일수·동선**. ⚠️ **레이아웃·행 구성 불변, 데이터만 교체** |
| B4 | 대표여정 **없는 도시**는 모달 대신 **바로 여정 생성**(가짜 카드 금지) |
| B5 | "대표 숏폼" 배지 = **영상이 실제로 있을 때만** |
| B6 | ⏸️ 대표 숏폼 영상 = **사장님 지시 대기** (5도시 × 60크레딧 = 300, 실원가 ~€18) |

**입증**: 사장님이 대표로 올린 여정의 **진짜 장소·사진**이 뜸. 파리 카드 재사용 0

### C. BTS 지구본 = 남은 공연도시 동적화
| # | 할 일 |
|---|---|
| C1 | `GET /api/bts/cities` 에 **"남은 공연" 필터** = `공연날짜 최댓값 ≥ 오늘`(archived 대신). `bts_rank` 순 + **공연장 좌표·공연시각 함께 반환** |
| C2 | 지구본 **좌표 13개 하드코딩 삭제**(§19) → C1 실제 좌표 |
| C3 | 웹 iframe **하드코딩 D-Day·날짜·경기장 삭제**(§19) → **날짜에서 D-Day 계산** |
| C4 | PlaceCart 상단 도시명과 **같은 소스 1벌 연동** = 지구본 선택이 8장 화면으로 이어짐 |
| C5 | BTSLanding `2026-04-09` 폴백 삭제(지난 날짜 = 거짓) |

**입증**: 지구본 **21개**, D-Day 오늘 기준, 지난 공연 안 보임, 지구본→8장 도시명 일치

### D. BTS 생성기 = 메인앱 1벌 통합 — ✅ **완료 (2026-07-31 as-built, 옛 D1~D11 계획 폐기 §19)**

> 구현 직전 사장님 교정 3건으로 계획이 바뀌어 **아래가 실제 지은 모습**이다:
> ① 결과 = **BTS 화면 안에서 화면 교체**(옛 BTSLoading·Dashboard 자리). **오버레이는 그냥 둠**(용도 = 메인앱 가서 다른 도시 생성·감상).
> ② BTS 도시 29곳(행수<300)은 어차피 정해진 도시 = **"작은 자체 db-only" 직행**.
> ③ **BTS "같이 떠나요" 생성 = 무료** — 크레딧 = 외부호출 발생 시만. db-only = 외부호출 0. (AI의견 = 외부호출 = 5 차감 유지)

**지은 것 (전부 커밋 전 실증 완료):**

| 무엇 | 어디 | 내용 |
|---|---|---|
| 새 화면 | `client/screens/bts/BTSTripScreen.tsx` | BTSContext 재료로 폼 조립 → **메인 여정화면 그대로**(`TripPlannerScreen initialRequest`) + 하단바(`aiOpensInPlace`) |
| 폼 자동생성 | `TripPlannerScreen`·`useTripPlanner` | `initialRequest` 있으면 입력화면 건너뛰고 **마운트 1회 자동 생성**(ref 잠금). 없으면 기존과 완전 동일 |
| 핀(고른 곳) | `ag2-gemini-recommender.ts` | 핀 SELECT 후 **rank −1 로 맨 앞** = 활동 컷 무조건 통과, 선택 순서 유지 |
| 핀 식당 | `ag4-db-finalize.ts` + `route-local.ts` | 식당풀에 무조건 합류 + `pickMealPriority` **핀 우선** = 고정 식사자리(점심 3번째·저녁 마지막)에 착석 |
| db-only 직행 | `pipeline-v3.ts` + ag2 관문 | 핀 있으면 행수<300 이어도 db-only (규칙 1벌 2곳) |
| 무료 | `city-place-routes.ts` | 핀 있으면 차감 안 함(단일 차감 지점 유지 §9) |
| 공연장 왕복 | (엔진 무변경) | **숙소 칸에 공연장 좌표** = 출발·도착 기점 + 지도 깃발 = "출발: MetLife Stadium" |
| 공연시각 | `BTSContext` +1줄, BTSTripScreen | 서버가 주던 `showTime` 을 드디어 읽음 → **endTime = 공연 3시간 전**(밥 먹고 입장) |
| 캐릭터→바이브 | `shared/vibe-category.ts` 신설 | `VIBE_PRIMARY_CATEGORY` 를 ag2 에서 **이동**(1벌 유지) + 가중치 내림차순 동적함수(하드코딩 맵 0) |
| AI의견 이중호출 잠금 | `useAiOpinionOverlay.ts` | **내 여정이 현재 여정일 때만** 신호 응답 = 안 보이는 벌이 같은 Gemini 호출을 또 내보내는 것 차단(위 경고의 이행) |
| 완전삭제 §19 | — | `BTSLoadingScreen`·`dashboard/`(ReelCard 포함)·**`BTSConcertPlannerScreen`**(옛 이벤트 화면 = 삭제 API 마지막 호출자)·`/api/bts/generate` 라우트·`bts-gemini.ts`·`MEMBER_NAMES`·BTSContext 의 itinerary 상태·죽은 i18n 키 11개×7언어 |
| 레이아웃 결함 수정 | 카트 `paddingBottom` 24→88 | [같이 떠나요]가 **하단 고정바에 덮여 안 눌렸다**(DevTools 좌표 실측). 4-24 여백 주석보다 7-30 "하단바 고정" 결정이 나중 = 최신 우선 |

**실증 결과 (이스트러더퍼드 = 82행 도시, 잔액 0 계정):**

| 검증 | 결과 |
|---|---|
| 핀 주입 | 서버 로그 `📌 핀 5/5곳 주입` |
| db-only | `_sourceMode: db-only`, 467ms, **Gemini 호출 0** |
| **무료** | 잔액 0 계정으로 **200 성공** + 장부 0줄·잔액 0 유지 (옛날이면 402) |
| 과금 회귀 없음 | 같은 계정 핀 없는 일반 생성 = **402** 그대로 |
| 동선 | 출발 깃발 = MetLife Stadium, 마지막 활동 = 공연장 옆 American Dream(귀소), 저녁 = 마지막 슬롯, 09:00~17:00(공연 20시 −3h) |
| 웹 E2E | 카트 5장 → 같이 떠나요 → 화면 교체 → 자동생성 1회(`POST /api/routes/generate` 정확히 1건) → 결과 표시, 콘솔 오류 0 |
| AI의견 | 결과 도달 시 하단 [AI의견]·[전문가] **자동 활성** + 누르면 **그 자리에서** 열림 + **호출 정확히 1회**(이중호출 잠금 입증) |

**검증(판단 3종 = §22)이 잡아 커밋 전 수정한 것 6건:**

| 게이트 | 적발 | 수정 |
|---|---|---|
| simplify | 🔴 캐릭터→vibe 를 **폐기 선언된 weights(5/3/2)** 로 만들어 카드 선정 기준과 어긋남 + Foodie 오염 | 정본 1:1(`CHARACTER_PRIMARY_CATEGORY`) 사용 = 카드와 같은 소스 1벌, `weights` 필드 완전삭제 §19 |
| simplify | ag4 핀 식당 쿼리를 통째 복붙(§16 위반) | 같은 쿼리 1벌 안 `OR 핀` 1줄로 통합 |
| react | 🔴 [일정] 창을 열면 새 벌이 전역 여정을 null 로 밟아 AI의견·전문가 **회색사** | "내가 올린 여정만 내린다" + 언마운트 정리 |
| 정확성 | 🔴 두 벌이 동시에 결과화면이면 전역 도장 **무한 핑퐁**(리렌더 폭주) | 전역값은 ref 로만 읽음(반응 안 함) — 이중결과 실재현 시험 = 1초 안정·에러 0 |
| 정확성 | 핀+미발견 도시 = 무료인데 **유료 MIX 로 새는 틈** | 핀 요청은 유료 경로로 절대 안 흘림(도시 미발견 = 즉시 실패) |
| 정확성 | BTS 결과 하단(공유·캘린더)이 고정바에 덮임 + 비로그인 자동생성 1회권 소진 | 안쪽 여백 = 바 높이 / 로그인 후에만 잠금·발사 |

**사장님 1차 검수(문제점 4건, 2026-07-31) → 전부 해소·실증:**

| # | 문제점 | 해소 |
|---|---|---|
| 1 | 결과화면 ← 가 메인 입력폼으로 = 카드로 돌아갈 길 없음(갇힘) | BTS 로 열렸으면 ← = **직전 카드 화면 복귀**(고른 카드 유지 = 재조정 후 재생성). E2E = 3장 유지 확인 |
| 2 | 캐릭터를 바꿔도 8장 카드가 전부 동일 | 원인 = 소도시 상위 rank 행이 멀티태그(American Dream 이 4개 카테고리 전부에 등장) → 카드 쿼리 정렬을 **주 카테고리 일치 우선**으로. 실측 = 컬렉터 박물관5 / 익스플로러 기념물5 / 리차저 정원5 / 칠러 쇼핑5 로 갈림. 부족분 = 시드 발굴로 자동 개선 |
| 3 | 최종 지도에 캐릭터 cat 마커가 떠야 함 | **이미 그렇게 그려지고 있었음**(지도 마커 = 카드와 같은 bts-marker-svg SSOT 재사용, 코드 0줄). DOM 실측 = heritage 갈색·healing 청록·공연장 보라(#9333ea)·출발 깃발 |
| 4 | 마지막 슬롯 = 무조건 공연장(공연 시작 시각) | 폼 2칸(`finalPlaceId`·`finalPlaceTime`) + ag4 가 마지막 날 끝에 **공연장 카드 1장 부착**(이름·사진·좌표·이동시간, 비용 0 = 결정⑥). 실측 = `20:00~23:00 🎤 MetLife Stadium` + 지도 보라 마커 |

**한계(정직 명시):** 느긋한 캐릭터 + 8곳 = 시간상 먼저 고른 만큼만 담김(결정 = 그냥 둠) / 소도시 식당 데이터가 얇아 저녁이 맨해튼까지 갈 수 있음(발굴이 진행되면 자동 개선) / 창에서 다른 도시 여정을 만들고 닫으면 뒤 BTS 의 AI의견이 회색(정직한 상태 = 마지막 여정의 주인이 사라짐, 크래시·이중호출 아님) / D10(자동 저장)·E1(BTS 탭 켜짐)은 이번 범위 밖.

### E. 하단 5단 = 메인앱 동일 + 어디서나 고정
| # | 할 일 |
|---|---|
| E1 | `activeTab` 에 `"BTS"` 키 추가 → BTS 안에서 현재 위치 켜짐 |
| E2 | **AI의견 탭 활성화**(사장님 ⑨) — D10 으로 비활성 해소. 백엔드 = **메인앱과 같은 `/api/itineraries/ai-opinion`·같은 프롬프트**, 화면 맥락만 다르게. 크레딧 5 차감 그대로 |
| E3 | `useNavigation` 타입 정정 |
| E4 | 지구본·아미봉 = **그대로 제외 유지** ✓ |

**입증**: BTS 3화면 전부 하단 5개 보이고 눌림. BTS 안에서 AI의견 결과 나옴 + 크레딧 5 차감 확인

### F. 인증 = 요소 동일 · 순서 = 생년월일 → 구글 → 카톡 → 애플 → 메일

> 🔴 **인증 상세는 여기 적지 않는다.** 정본 = [`docs/2026-07-25 구글·카카오 인증 실연결.md`](2026-07-25%20구글·카카오%20인증%20실연결.md) 의 【2026-07-31】 절.
> 사유(사장님 지시 2026-07-31): 인증 기록이 여러 문서에 흩어져 사장님이 찾기 어려웠다 = **한 파일에만 쌓는다**(§19 2벌 공존 금지).

| # | 할 일 | 상태 |
|---|---|---|
| F1 | 메인앱 인증창 순서 교정 | ✅ **완료** |
| F2 | 애플 로그인 신규 구현(네이티브) | ✅ **코드 완료** (실기기 = 빌드 후) |
| F3 | 기기별 분기 — 애플 iOS 노출 / AOS·웹 비노출 | ✅ **완료** |
| F4 | BTS 랜딩 바이패스 삭제(§19) → 진짜 로그인 | ✅ **완료** |
| F5 | BTS 화면 `/api/bts/*` 인증·차감 | ⏸️ D5 로 자동 해소 예정 |

**번들ID = `com.sonanie.guide`**(iOS만. 안드로이드 `com.vibetrip.app` 불변). 출처 = `app.json` 1곳.
**콘솔 3곳(애플·구글·카카오) = 등록 완료 = 새로 만들 것 없음.**

### G. iOS 빌드 (제가 함)
| # | 할 일 | 누가 |
|---|---|---|
| G1 | `app.json` `ios.bundleIdentifier` → **`com.sonanie.guide`**. ⚠️ **`android.package` 는 손대지 않음**(안드로이드 카카오 2026-07-28 성공 보존) | 저 |
| G2 | 애플 등록·서명 = EAS. 아이폰12(`R6GP7YSN8U`)는 fable5 가 등록 완료 | 저 (실패 시 사장님 애플 로그인) |
| G3 | 개발빌드 → 아이폰12. **내손앱 아이콘 대체**(승인됨) | 저 |
| G4 | 콘솔 2곳에 `com.sonanie.guide` — 구글 iOS 클라이언트, 카카오 iOS 플랫폼 | **사장님** |
| G5 | 아이폰 구글·카톡·애플 실기기 확인 | **사장님** |

⚠️ **핵심**: 카카오·구글·애플 로그인은 **전부 네이티브 모듈**이고 `app.json` 에 `withKakaoNative`·`withGoogleNative` 가 있음. **네이티브는 Expo Go 에서 안 돔** → 아이폰을 Expo Go 로 테스트하면 원리적으로 실패. **개발빌드가 정답.**

### H. 마무리
묶음마다 기계 4종 + 브라우저 실증 + DB 실측 → 이 문서 as-built 갱신 → 판단 3종 → **커밋은 사장님 지시 시** → main 은 `merge` 만(rebase·reset 금지)

---

## 4. 순서

```
1단계  A (도시버튼 5개)        ─┐
2단계  C (지구본 21개)          ─┼─ 독립 = 병행
3단계  G1 + F2 (번들ID·애플)    ─┘ 빌드가 걸려 먼저 착수
        ↓
4단계  B (베스트갤러리)  ← A 결과 사용
        ↓
5단계  D1·D2·D3·D6 (고른곳·공연장왕복·식당자리) ← 단독 입증 필수
        ↓
6단계  D4·D5 (Loading 삭제 + 생성기 교체) → D7~D11
        ↓
7단계  E (하단 5단 + AI의견 활성) ← D10 연결
        ↓
8단계  F1·F3·F4·F5 (인증) + G2~G5 (빌드·실기기)
```

**5단계 단독 입증 후 6단계** — 여정 생성은 앱의 심장. 파리 여정 1건으로 **전후 비교**.

---

## 5. 리스크 · 한계

| # | 항목 |
|---|---|
| 1 | **D1 이 AG2 를 건드림.** 최소 변경이지만 여정 생성 = 앱의 심장. 단독 입증 후 진행 |
| 2 | **어느 날짜에 배치될지는 지리(sector)가 결정.** BTS 는 1일이라 무관하나, 메인앱 다일정은 그대로 |
| 3 | **`bts_archived` 를 고치지 않고 날짜 기준으로 우회.** 플래그 갱신은 DB 쓰기 = 사장님 승인 필요 |
| 4 | **애플 로그인은 네이티브 = OTA 불가.** 반드시 빌드와 함께. iOS 만 지원 |
| 5 | **G3 = 내손앱 아이콘 대체.** 승인됨. 되돌리려면 앱스토어 재설치 |
| 6 | fable5 가 인증 파일을 동시에 만지면 충돌. **F·G 를 제가 맡으니 역할 정리 필요** |
| 7 | 워크트리에 **결제 외 FE 3,110줄**(지구본 웹 iframe 306줄 포함)이 있고 제가 검증 안 함. main 병합 시 회귀는 이번 작업과 분리 판단 |
| 8 | iOS 실기기 최종확인 = 사장님 몫(이 PC = 윈도우) |

---

## 6. 개발이력

| 날짜 | 내용 |
|---|---|
| 2026-07-29~30 | 결제·크레딧 백엔드 + 프로필 FE 완료·검증. 판단 3종이 제 결함 9건 적발(치명 = 차감 3곳이 토큰 미첨부로 무료) → 전부 확인 후 수정 |
| **2026-07-30** | 사장님 로컬 5055 확인 → **"90% SSOT 대로 구현됨"** |
| **2026-07-30** | 실측: 완비도시 **5개**(사장님 "5개" 일치) / BTS **34도시 + 공연장 좌표 32 + 공연시각 + 아미존·머치스토어 각 32 가 이미 DB 에 있음** / `bts_archived` 낡음(2개만 true, 실제 지난 공연 13) / **애플 로그인 앱 전체에 없음**(키만 있음) / 인증창 JSX 순서가 **자기 SSOT 주석과 반대** |
| **2026-07-30** | 제 과대평가·오판 3건 정정 — ㉠ "고른 곳 고정" 은 2군데 수정으로 끝남(BTS 에 이미 패턴 존재) ㉡ "식당 고정은 한계" 철회 = 점심·저녁 자리가 **이미 고정 구현**돼 있음 ㉢ 하단 탭 클릭 불가 = 창 크기 아님, **오버레이가 덮는 것**이고 main 과 동일 |
| **2026-07-30** | 사장님 결정 ①~⑬. `BTSLoading` **삭제**, BTS 는 **1인·1일**, 비용은 **소용 없음**, `ReelCard` 이미지 **유지**, 인증 순서 **생년월일→구글→카톡→애플→메일**(애플 = iOS 만 노출·공간 확보) |
| **2026-07-30** | 아이폰12 피켓 잘림 수정 — 원인 = 피켓이 지구본 상자 **안**이라 배율이 **곱해짐**(277 × 2.6 × 1.5 = **1080px**, 390px 화면의 2.8배). 해법 = 지구본 배율의 역수로 상쇄 + 화면 너비에 묶음. 실측 = 넘침 **좌 344·우 346 → 0** |
| **2026-07-30** | **지구본 라벨 부품 에셋화**(사장님 지시 = 추후 여정플래너 도시 선택에 활용). `client/components/ui/GlobeLabelsWeb.tsx` 신설 + 원본 보존 `docs/design-references/cobe-globe-labels-reference.tsx`. 원본에서 바꾼 것 = Tailwind 제거 · **라벨을 누를 수 있게**(onSelect, 도시 선택 필수) · 배율 상쇄(parentScale) · 좌표/이름만 넘기면 되게 일반화. **아직 어디에도 안 붙임**(부품만) |
| **2026-07-30** 커밋 `0c26e0c` | 검증 7단계가 **결함 7건** 적발·수정 — 결제 이중충전 열쇠가 스키마에 없어 `drizzle-kit push` 한 번에 지워질 수 있었음(€10 에 280 크레딧) / 앱 지도가 항상 대서양으로 줌인 / 정리 안 되는 애니 루프 / 줌인 대상 판단 2벌 / D-Day 글자 규칙 2벌(`D-0`·`D--3`) 등. 죽은 파일 10개(3,094줄) 삭제 |
| **2026-07-30** 커밋 `599faca` | **사장님 아이폰 실기기 지적 4건 해소** — ㉠ 하단 5버튼이 안드로이드에서만 BTS 를 닫아버림(= `fullScreenModal` 은 아이폰 전용, 안드로이드는 `modal` 로 낮춰져 뒤 화면 제거) → **`transparentModal`** 로 교체 ㉡ 앱에서 지구본이 안 뜨고 옛 SVG 평면지도가 뜸 → **웹·앱 같은 HTML 1벌**(WebView), 옛 자산 2,315줄 삭제 ㉢㉣ 체험버튼·build-02 삭제. 함께 = 배지 조회 폭주 차단(초당 5~8회 → 3초 안 1회) · AI의견/전문가 = 여정 있을 때만 활성 · 삼각뿔·도시 점 삭제 · APK 자동빌드 끄고 수동만. 검증 3회에서 **결함 13건** 적발·전부 수정. 순 **−2,588줄** |
| **2026-07-30** | 진행상태 문서를 **내용 기준으로 재작성** → [`2026-07-30 BTS 지구본·미니앱 구조 SSOT.md`](2026-07-30%20BTS%20지구본·미니앱%20구조%20SSOT.md). 이름이 "인수인계"라 나중에 내용을 알 수 없다는 사장님 지적 반영 |
| **2026-07-31** | **D단계(BTS 여정 생성) 완료** — 위 D절 as-built 참조. 계획 대비 사장님 교정 3건(화면 교체 방식·db-only 직행·**생성 무료**)이 구현 전에 반영됨. 실증 = 이스트러더퍼드(82행·잔액 0 계정) 핀 5/5·Gemini 0·무료(장부 0줄)·일반 생성 402 유지·웹 E2E 생성 1회·AI의견 그 자리 1회 호출. 부수 발견·수정 = [같이 떠나요]가 하단바에 덮여 안 눌리던 결함(여백 24→88) + 옛 이벤트 화면 `BTSConcertPlanner` = 삭제된 API 의 마지막 호출자라 함께 완전삭제 §19 |
