// ⚠️ 수정금지(승인필요) 2026-09-07 사장님 결정 = 접속 현황 자료 = shared/admin-access-summary 1벌을 그대로 쓴다(§16 재발명 금지).
//   이 파일은 §0 파일 700줄 상한 때문에 routes-admin.ts 에서 분리한 얇은 통로다.
export {
  accessSummary,
  type AccessSummary,
} from "../shared/admin-access-summary";
