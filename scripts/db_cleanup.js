import { db } from '../server/db.js'; import { sql } from 'drizzle-orm'; async function checkSpace() { const res = await db.execute(sql\SELECT relname as \\	able\\, pg_size_pretty(pg_total_relation_size(relid)) as \\size\\, pg_total_relation_size(relid) as \\ytes\\ FROM pg_catalog.pg_statio_user_tables ORDER BY pg_total_relation_size(relid) DESC LIMIT 20;\); console.log(res); process.exit(0); } checkSpace().catch(console.error);