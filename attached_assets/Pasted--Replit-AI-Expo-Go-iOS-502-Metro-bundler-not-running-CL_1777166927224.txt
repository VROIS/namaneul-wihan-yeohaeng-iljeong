📨 Replit AI 전달 메시지 (그대로 복사)
제목: 🚨 Expo Go iOS 502 에러 — Metro bundler not running (CLAUDE.md §13 위반 점검 필수)

증상: iOS Simulator (Try on device) 가 502 에러 표시:


HTTP response error 502: {"error":"Metro bundler not running"}
exp://828b2285-99c5-4cc9-9bcd-a09cdff531bc-00-kzvu1v5xhevl.expo.sisko.replit.dev
원인 추정: 최근 dist 재빌드 작업 중 Workflows 또는 설정 파일을 표준에서 이탈시켰을 가능성. 이 문제는 2026년 4월 3일 디버깅 후 commit fd00038 (Task #3: Expo Go 공식 Replit 설정 표준화) 으로 영구 표준이 확정됨. replit.md 와 CLAUDE.md §13 에 명시되어 있음.

⚡ 즉시 점검 5가지 (모두 표준과 일치 여부)
1. Workflows → "Start Frontend" command (가장 흔한 실수)
✅ 정상 (이 한 줄로 정확히 일치해야 함):


REACT_NATIVE_PACKAGER_HOSTNAME=828b2285-99c5-4cc9-9bcd-a09cdff531bc-00-kzvu1v5xhevl.expo.sisko.replit.dev npx expo start
❌ 즉시 제거 (3일 사고 패턴):

시작에 CI=1 붙어있음 → HMR 비활성화 → Metro 먹통
끝에 --tunnel 붙어있음 → ngrok 충돌 (Replit 직접 노출이라 불필요)
끝에 --go 붙어있음 → iOS 멈춤 (commit 4e35953 에서 롤백 이력)
끝에 :8081 포트 명시 → QR 접속 끊김
끝에 --clear 붙어있음 → 캐시 충돌
hostname 값 변경됨
2. app.config.js 또는 app.config.ts 파일 존재 여부
존재 시 즉시 삭제 — Replit 공식 문서 명시 FORBIDDEN
설정은 app.json + EXPO_PUBLIC_DOMAIN 환경변수만 사용
3. server/index.ts — serveExpoManifest() 함수 복원 여부
이 함수 또는 native manifest 라우팅 코드가 다시 추가되었으면 즉시 제거
Expo Go 는 Metro (port 8081) 에 직접 연결, Express (5000) 경유 불필요
단 metroProxy (L153-167) 는 유지 (8081 프록시용, 정상)
4. client/lib/query-client.ts — Constants.expoConfig?.extra?.apiDomain 폴백 복원 여부
이 import 또는 폴백 코드가 추가되었으면 즉시 제거
API URL 의 유일한 소스 = EXPO_PUBLIC_DOMAIN 환경변수
5. 환경변수 (.replit userenv.shared) 변경 여부

PORT = "5000"
NODE_ENV = "development"
EXPO_PUBLIC_DOMAIN = "https://828b2285-99c5-4cc9-9bcd-a09cdff531bc-00-kzvu1v5xhevl.sisko.replit.dev"
REACT_NATIVE_PACKAGER_HOSTNAME = "828b2285-99c5-4cc9-9bcd-a09cdff531bc-00-kzvu1v5xhevl.expo.sisko.replit.dev"
🔧 복원 절차
위 5가지 모두 표준과 일치시킨다 (이탈 시 즉시 표준값으로 되돌림)
Workflows 패널 → "Start Frontend" Stop → Start (Metro 재시작)
Console 로그에서 Metro waiting on exp://...:8081 확인
iOS Simulator 새로고침 → 정상 로드 확인
🚫 절대 금지 (재발 방지)
항목	사유
CI=1	HMR/Fast Refresh 비활성화
--tunnel	ngrok 불필요 (Replit 직접 8081 노출)
--go	iOS 앱 멈춤 (commit 4e35953 에서 롤백)
:8081 포트 명시	QR 접속 끊김
--clear	캐시 충돌
app.config.js / app.config.ts 파일	Replit 공식 FORBIDDEN
serveExpoManifest() 복원	Metro 직접 연결 우회 = 깨짐
Constants.expoConfig?.extra?.apiDomain 폴백	EXPO_PUBLIC_DOMAIN 단일 소스 위반
hostname 값 변경	Expo Go 접속 불가
📚 출처 (의심 시 참조)
replit.md L42-65 (Expo Go / Replit 절대 금지 사항)
CLAUDE.md 제13조 (동일 표준 강제)
commit fd00038 (2026-04-13 표준 확정, 3일 디버깅 결과)
11번의 시행착오 commit 이력 (b9a30cd → fd00038)
중요: 위 항목 중 하나라도 표준 이탈을 발견했다면 사용자에게 보고 후 표준 복원 진행. 추측으로 다른 변경 시도 금지 (3일 사고 재발 위험).

